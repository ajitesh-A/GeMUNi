{
  "name": "gemuni-ai",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel -r dev",
    "build": "pnpm run -r build",
    "lint": "pnpm run -r lint",
    "clean": "pnpm run -r clean"
  },
  "engines": {
    "node": ">=18"
  }
}
